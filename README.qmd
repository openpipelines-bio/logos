---
title: OpenPipeline logo contest
format: gfm
---

```{r echo=FALSE, output="asis"}
svgs <- list.files(".", pattern = "*.svg")

for (svg in svgs) {
  cat(glue::glue("

## `{svg}`

![]({svg})

"), sep = "")
}
```